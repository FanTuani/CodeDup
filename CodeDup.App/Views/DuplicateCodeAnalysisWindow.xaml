<Window x:Class="CodeDup.App.Views.DuplicateCodeAnalysisWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="重复代码分析" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- 参数设置 -->
        <GroupBox Header="分析参数" Grid.Row="0" Margin="10">
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock Text="最小重复次数：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="MinOccurrencesBox" Width="60" Text="3" VerticalAlignment="Center"/>
                <TextBlock Text="（出现在多少个不同文件中）" VerticalAlignment="Center" Margin="10,0,20,0" Foreground="Gray"/>
                
                <TextBlock Text="最小代码行数：" VerticalAlignment="Center" Margin="0,0,5,0"/>
                <TextBox x:Name="MinLineCountBox" Width="60" Text="3" VerticalAlignment="Center"/>
                <TextBlock Text="（连续多少行视为一个片段）" VerticalAlignment="Center" Margin="10,0,20,0" Foreground="Gray"/>
                
                <Button Content="开始分析" Click="Analyze_Click" Padding="15,5"/>
            </StackPanel>
        </GroupBox>
        
        <!-- 统计信息 -->
        <GroupBox Header="分析结果" Grid.Row="1" Margin="10,0,10,10">
            <StackPanel Orientation="Horizontal" Margin="10">
                <TextBlock x:Name="StatsText" Text="等待分析..." FontSize="14"/>
            </StackPanel>
        </GroupBox>
        
        <!-- 重复代码列表 -->
        <GroupBox Header="重复代码片段" Grid.Row="2" Margin="10,0,10,10">
            <ListView x:Name="FragmentList" SelectionChanged="FragmentList_SelectionChanged">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="重复次数" Width="100" DisplayMemberBinding="{Binding OccurrenceCount}"/>
                        <GridViewColumn Header="代码行数" Width="100" DisplayMemberBinding="{Binding LineCount}"/>
                        <GridViewColumn Header="代码预览" Width="400" DisplayMemberBinding="{Binding PreviewText}"/>
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>
        
        <!-- 详细信息 -->
        <GroupBox Header="详细信息" Grid.Row="3" Margin="10,0,10,10" Height="150">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <GroupBox Header="出现位置" Grid.Column="0" Margin="0,0,5,0">
                    <ListBox x:Name="LocationList" DisplayMemberPath="LocationText"/>
                </GroupBox>
                
                <GroupBox Header="完整代码" Grid.Column="1" Margin="5,0,0,0">
                    <TextBox x:Name="CodePreviewBox" 
                             IsReadOnly="True" 
                             VerticalScrollBarVisibility="Auto"
                             FontFamily="Consolas"
                             FontSize="12"
                             TextWrapping="NoWrap"/>
                </GroupBox>
            </Grid>
        </GroupBox>
    </Grid>
</Window>

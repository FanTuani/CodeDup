using System; using System.Collections.Generic; using System.Linq; class Time { private int hour, minute, second; private string timeSystem; public bool flag; public Time(int hour, int minute, int second) { this.hour = hour; this.minute = minute; this.second = second; this.timeSystem = ""; } public int Hour { get { if (!flag) { timeSystem = ""; return hour; } if (hour < 12) { timeSystem = "am"; return hour; } else { timeSystem = "pm"; return hour - 12; } } } public void Print() { int re = Hour; if (flag) Console.WriteLine($"{timeSystem} {Hour}:{minute}:{second}"); else Console.WriteLine($"{Hour}:{minute}:{second}"); } public void AddSecond() { second++; if (second >= 60) { second = 0; minute++; if (minute >= 60) { minute = 0; hour++; if (hour >= 24) hour = 0; } } } } class Program { static void Main() { string[] s = Console.ReadLine().Split(' '); int hour = int.Parse(s[0]); int minute = int.Parse(s[1]); int second = int.Parse(s[2]); if (hour < 0 || hour > 23 || minute < 0 || minute > 59 || second < 0 || second > 59) { Console.WriteLine("Inputting illegal characters"); return; } var t = new Time(hour, minute, second); string str = Console.ReadLine(); t.flag = bool.Parse(str); s = Console.ReadLine().Split(' '); for (int i = 0; i < s.Length; i++) { t.AddSecond(); } t.Print(); } }
using System; using System.Data.SqlClient; class Program { static void Main() { using (var conn = new SqlConnection("Server=localhost;Database=DB;Trusted_Connection=True")) { string sql = "SELECT Name, Age FROM Stu"; var cmd = new SqlCommand(sql, conn); conn.Open(); SqlDataReader reader = cmd.ExecuteReader(); while(reader.Read()) { string str = reader["name"]; } reader.Close(); } } }
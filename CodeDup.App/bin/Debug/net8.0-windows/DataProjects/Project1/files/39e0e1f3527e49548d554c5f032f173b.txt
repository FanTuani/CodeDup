using System; using System.Data.SqlClient; class Program { static void Main() { string connStr = "Server=localhost;Database=StudentDB;Trusted_Connection=True;"; using (SqlConnection conn = new SqlConnection(connStr)) { try { string sql = "SELECT Name, Age FROM Students"; SqlCommand cmd = new SqlCommand(sql, conn); conn.Open(); SqlDataReader reader = cmd.ExecuteReader(); while (reader.Read()) { Console.WriteLine("Name: {0}, Age: {1}", reader["Name"], reader["Age"]); } reader.Close(); } catch (Exception ex) { Console.WriteLine("发生异常：" + ex.Message); } } } } class FK { private int age; public int Age { get { return age; } set { age = value; } } }